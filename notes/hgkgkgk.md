---
timezone: UTC+8
---

# hgkgkgk

**GitHub ID:** hgkgkgk

**Telegram:** @hgkgkgk

## Self-introduction

AI x Web3 School

## Notes

<!-- Content_START -->
# 2026-05-24
<!-- DAILY_CHECKIN_2026-05-24_START -->
```
今天学习了 AI 中的 Context 和 Web3 中的 Network。我目前的理解是：模型本身并不直接知道真实世界，它只能根据当前拿到的上下文生成结果，因此上下文的质量会直接影响输出可靠性；而在 Web3 中，链上状态、资产和交易语义都依赖于具体网络。今天最大的收获是，我开始意识到 AI Agent 在 Web3 场景下不能只“理解语言”，还必须“理解当前是哪条链、数据来自哪里、哪些是事实哪些是推断”。目前我还想继续弄清楚，链感知上下文在真实 Agent 工作流中应该如何组织和引用。明天我准备继续学习相关内容。
```
<!-- DAILY_CHECKIN_2026-05-24_END -->

# 2026-05-22
<!-- DAILY_CHECKIN_2026-05-22_START -->

```
## 今日学习内容

### AI 基础：上下文（Context）

学习了 Context 是模型在当前请求里能看到的全部信息空间。核心收获：LLM 不会自动连接数据库、API 或文件系统，它只基于当前上下文生成输出。如果上下文错了、缺了、过期了，模型再强也会给出不可靠答案。

三个关键概念：
- **Context Window**：模型一次请求能处理的最大上下文范围，窗口大 ≠ 会用得好
- **Context Engineering**：设计上下文进入模型的方式，包括选数据源、排序、裁剪、标注可信度
- **Memory**：跨请求保留的信息（偏好、历史），但不能替代实时授权

Agent 上下文的最佳分层：指令层 → 任务层 → 事实层 → 知识层 → 记忆层。

### Web3 基础：钱包（Wallet）

学习了钱包不是"登录按钮"，而是用户管理账户、签名授权、发送交易、切换网络和确认风险的入口。

三类钱包动作：
- **连接钱包**：读取地址和网络，≠ 可以动用资产
- **签名消息**：证明控制权，可能用于登录或授权
- **发送交易**：改变链上状态，风险最高

核心概念：EOA（私钥控制的外部账户）、助记词（不应被任何应用索要）、Transaction（点击按钮≠交易完成）、Gas（链上执行成本）、Explorer（查看链上事实的窗口）。

## AI × Web3 连接点

AI Agent 要帮用户完成链上操作，为什么既需要上下文，又需要钱包？

**上下文负责"理解"**：Agent 要知道用户想干什么、当前在哪个网络、有什么历史状态、系统规则是什么。没有上下文，Agent 只能猜，猜就会错。好的上下文设计让 Agent 分得清指令层、事实层和记忆层，不会把过期信息当最新规则。

**钱包负责"执行"**：Agent 理解任务后，需要某种方式代表用户发起链上操作。钱包提供地址（身份）、签名（授权）和交易（执行）能力。但这里的关键是：Agent 可以准备参数、检查风险、生成操作计划，但签名和权限不能被直接交给模型。

**核心边界**：上下文让 Agent 知道该做什么，钱包让 Agent 能做什么，但两者必须共同设边界——上下文隔离不可信来源，钱包保留人工确认最后一步。AI 做理解和辅助，钱包负责确认和授权，智能账户负责执行约束。

## 今日疑问
- 在 GP-BFL 这种区块链辅助联邦学习场景里，我自己的训练节点的上下文应该包含哪些层级？节点的历史信誉和当前行为证据，是不是其实就是在构建一种"Agent 上下文"？

## 明日计划
- 阅读 AI × Web3 Bridge：智能体钱包（Agent Wallet）
- 继续理解 Agent 如何与链上执行交互
```
<!-- DAILY_CHECKIN_2026-05-22_END -->

# 2026-05-21
<!-- DAILY_CHECKIN_2026-05-21_START -->


### 1\. LLM 的概率本质决定了它在系统里的位置

LLM 生成的是概率合理的输出，不是天然可信的事实。Token、Embedding、Transformer、Hallucination 这些概念背后有一条共识：**模型做理解和生成，但不做最终验证**。越靠近执行层，越要把自然语言输出变成可检查对象。

### 2\. Prompt 是任务边界，不是咒语

Prompt 的核心价值是区分"解释"和"执行"——告诉模型能做什么、不能做什么、不确定时怎么处理。Structured Output 不是为了好看，是为了让后续代码能检查、拒绝、记录。Prompt Injection 的防护不是写一句"不要被注入"，而是把指令、上下文和工具权限隔离开。

### 3\. 钱包管理的是链上控制权，不是账号资料

连接钱包、签名消息、发送交易是三类完全不同的动作，风险也不同。EOA 简单但权限粗；Mnemonic 是高风险秘密；交易从按钮到上链是多步流程。核心原则：**AI 做辅助，钱包做确认**。
<!-- DAILY_CHECKIN_2026-05-21_END -->

# 2026-05-20
<!-- DAILY_CHECKIN_2026-05-20_START -->



今天读了 AI × Web3 Handbook 的三个核心页面：LLM、Prompt 和 Wallet。

> 最大的收获是找到了一个跨越 AI 和 Web3 的共同设计原则：**把"理解/辅助能力"和"签名/执行授权"分开**。LLM 可以解释交易但不能替用户确认，钱包可以签名但不能替用户判断。
> 
> 今天的疑问是：当 AI Agent 真正进入链上执行时，用户端的 UI/UX 如何让人理解"AI 的判断"和"你本人的决定"之间的边界。
<!-- DAILY_CHECKIN_2026-05-20_END -->

# 2026-05-18
<!-- DAILY_CHECKIN_2026-05-18_START -->




![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/AI-Web3-School/main/assets/hgkgkgk/images/2026-05-18-1779114282348-image.png)![image.png](https://raw.githubusercontent.com/IntensiveCoLearning/AI-Web3-School/main/assets/hgkgkgk/images/2026-05-18-1779115064415-image.png)

成功安装了Hermes Agent 并连接上了tg
<!-- DAILY_CHECKIN_2026-05-18_END -->
<!-- Content_END -->
